# Sample ROS 2 package

It is a sample package that holds **subscriber** node written in C++ and 
**publisher** node written in python language. 

Before running the package, make sure that scripts/hello.py has privilleges to be executed ( check using ls -vl command and set privilliges using chmod +x command).